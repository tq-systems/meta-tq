From dde478b0233bc77f5d4469ab197e5f620eddc19a Mon Sep 17 00:00:00 2001
From: Markus Niebel <Markus.Niebel@tq-group.com>
Date: Fri, 11 Jan 2019 09:40:27 +0100
Subject: [PATCH] board: tqma8mx: debug spl

Signed-off-by: Markus Niebel <Markus.Niebel@tq-group.com>
---
 board/tqc/tqma8mx/spl.c | 6 +++++-
 1 file changed, 5 insertions(+), 1 deletion(-)

diff --git a/board/tqc/tqma8mx/spl.c b/board/tqc/tqma8mx/spl.c
index 68c1feeb88..f5dea67f9f 100644
--- a/board/tqc/tqma8mx/spl.c
+++ b/board/tqc/tqma8mx/spl.c
@@ -31,6 +31,7 @@ DECLARE_GLOBAL_DATA_PTR;
 
 void spl_dram_init(void)
 {
+	puts("spl_dram_init\n");
 	/* ddr init */
 	if ((get_cpu_rev() & 0xfff) == CHIP_REV_2_1)
 		ddr_init(&dram_timing);
@@ -100,6 +101,8 @@ static struct fsl_esdhc_cfg usdhc1_cfg = {
 int board_mmc_init(bd_t *bis)
 {
 	int ret;
+
+	puts("board_mmc_init\n");
 	/*
 	 * According to the board_mmc_init() the following map is done:
 	 * (U-Boot device node)    (Physical Port)
@@ -168,6 +171,7 @@ int power_init_board(void)
 
 void spl_board_init(void)
 {
+	puts("spl_board_init\n");
 #ifndef CONFIG_SPL_USB_SDP_SUPPORT
 	/* Serial download mode */
 	if (is_usb_boot()) {
@@ -212,7 +216,7 @@ void board_init_f(ulong dummy)
 	timer_init();
 
 	preloader_console_init();
-
+	puts("TQMa8MQ SPL\n");
 	/* Clear the BSS. */
 	memset(__bss_start, 0, __bss_end - __bss_start);
 
